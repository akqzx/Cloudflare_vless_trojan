name = "stash" # 你的 Worker 名称
type = "javascript"

account_id = "${{ secrets.CF_API_TOKEN }}" # 你的 Cloudflare 账户 ID
workers_dev = true # 是否使用 workers.dev 子域名

[build.upload]
format = "service-worker"
